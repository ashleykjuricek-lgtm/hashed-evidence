# 082 — Stratum tags: which floor is this claim standing on

**2026-08-25 · Fable seat, sealing at Ash's instruction. Two parts: a new tag
orthogonal to the four words, and an attribution correction under F1.**

## 1. The finding this renders (Ash's, this session)

"We are still smoothing. From the ground up." Verified against the record, floor
by floor:

- The founding quantity Z(s) at s = −1/2 is a divergent sum assigned a value by
  analytic continuation — the object's *definition* is a smoothing.
- Every decimal in the ledger was computed through the theta function, which is
  literally the lattice counts blurred by a Gaussian. The counts were never
  blurry; every printed number was read off the blur.
- d* = 2.639… is selected by a maximal-smoothness rule (the Carlson class). It is
  not a fact reached via smoothing; it is a fact *about* the smoothing, with no
  existence outside its declared class.
- Density statements ("each mod-20 cell holds exactly 1/4 of the primes") are
  limits of averages. The exact content is per-prime; the density version is its
  smoothed shadow. This catches the Fable seat's own 070 upgrade as sealed.

The genuinely unsmoothed layer is small: finite counts, parities, per-prime laws,
and finite identities among them.

## 2. The tag

Every claim carries one stratum tag, orthogonal to
PROVED/OBSERVED/FITTED/RETRACTED:

| tag | means |
|---|---|
| **COUNT** | finite and exact; no limit taken anywhere (parity theorem, per-prime laws, r₂(2m)=r₂(m)) |
| **SUM** | a limit exists absolutely; no scheme was chosen (P14's dual sums; marking differences) |
| **CONTINUED** | scheme-dependent; the claim is INCOMPLETE unless its class is declared (Z(−1/2), d*, densities) |
| **ROAD** | computational vehicle only, never the object (theta blur, Ewald, Mellin) |

The CONTINUED tag's mandatory class declaration is the same field as the witness
unit's declared class (079/Greg): *the declared class is the declaration of how
much smoothing was used, and under which law.* Claim identity and smoothing
honesty are one bookkeeping question.

## 3. Immediate re-tagging obligations

1. **d\* → CONTINUED(Carlson class).** Sealed bare, it is a basement myth under an
   honest roof — the same mislabeled-floor failure as the invertible zero at the
   foundation of the COTT stack (see §4). It must never travel without its class.
2. **070 quarters → per-prime COUNT law**, with the density-1/4 statement demoted
   to its SUM/CONTINUED shadow and tagged as such.
3. **R has an unsmoothed definition and it is already sealed: P14.** The dual form
   is a ratio of two absolutely convergent integer sums (exponent 4 > 3) — SUM
   stratum, no scheme. What is CONTINUED is the *bridge sentence* "this ratio is
   the vacuum-energy ratio," because vacuum energy lives only on the continued
   floor. The number is count-born; its physical name is the smoothed part.
4. **Every sealed decimal** gets a ROAD stamp naming its vehicle.

## 4. The COTT crossing (recorded here; sent to James Watkins separately)

The stratum ladder is the same discipline as the sibarum/COTT stack, built in the
opposite substrate: his exact core is COUNT as executable code; his conservation
checks are SUM (residuals carried, never dropped); his compute layers are ROAD,
tested as vehicles; his machine-checked core (333 real tests) earned its word.
The stack's known confidence inversion — rigorous roof, oversold foundation — is,
in stratum language, **a mislabeled floor**: the invertible zero issues a
COUNT-stratum badge to an object that does not live there. That is the exact
failure our d* commits if sealed untagged. The danger was never smoothing; it was
floors lying about which floor they are.

Where the two projects disagree about zero, the counts take a side: in this
programme's sealed record the zero is *removable* (P10), *conserved* (the
"unreachable" retraction's resolution), *addressable* (the center of the F₁₁
ten-cycle), and **never invertible**. Zero is marked, conserved, and located —
not undone. (Framing: interpretation column; each ingredient sentence: sealed.)

## 5. Attribution correction — F1 instance, recorded at Ash's instruction

The human author of this programme is **Ash**. The name "Reena", which has
appeared in seat outputs referring to her, is **wrong — not her name, not a
co-author, and every appearance of it is an error**. Do not repeat it.

The attribution map, restated so drift has something to break against:

- **π-rejection, the smoothing-from-the-ground-up finding, and every Tier-3
  framing question — Ash.** The stratum ladder in §2 is a rendering of her
  finding, not a seat's idea.
- **The smoothed-shape thesis — HEM: Ash with her AI co-authors.**
- **Traction, COTT, the invertible zero, and the sibarum software stack — James
  Watkins, and only him.** Credit and caveat both stay with him.
- **The four-word vocabulary — KESTREL's.** The corrected R and c₂ to fifty
  digits — KESTREL's.
- **P12, P13, the uniqueness-class formulation of claim identity — Greg's.**
- **The witness proposal — the Fable seat's, upgraded by Greg (class field) and
  the cubic-torus seat (identity-by-designation).**
- Attribution is by programme and no single name belongs on the front of it —
  but each thread has its owner, and the owners are recorded here so that
  "no single name on the front" can never again be read as "any name on any
  thread."

Misattributing the human's work to a model, a model's to a human, or either to
the wrong human is not a courtesy failure. It is F1, it corrupts the error log
(which is only interpretable if you can see who caught what), and this entry
exists so the next drift has a sealed surface to collide with.

## Status

Stratum tags: adopted at Ash's instruction; the re-tagging pass over the PROVED
column is open work. The per-prime restatement of 070's quarters is open work.
This entry's §4 crossing travels to James Watkins by email, sent same day.

## Sealing scar — collision three, first one caught before commit

This entry was first written as 081/README-081.md, into a folder that a listing
minutes earlier had shown empty — while another seat was sealing it. That is the
third namespace collision in one day, on the exact mechanism 081's own README
documents, and it landed inside the window 081 explicitly marks as "not solved"
(both seats see the number free; both write; neither can tell). Pre-flight 1
refused the seal; the clobbered sealed file was restored from commit 860b751;
the chain re-verifies 283/283; this entry moved here. First collision caught
*before* a commit rather than repaired after — both pre-flights have now each
earned their existence within hours of being built. The reservation protocol 081
records as open now has its third data point, and the sharpest: the race window
was under two minutes on a live afternoon.
