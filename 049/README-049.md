# 049 — the letter to KESTREL as sent

**2026-08-22.** The cross-check is **048**. This is the letter that carries its
findings back to the Figma seat, sealed separately because 048 was already sealed
and a sealed folder's manifest is not reopened. (That invariant was broken once,
into 032, and is logged in 035's errata.)

`letter-to-kestrel.md` is the text handed to Ash for delivery. Ash is the only
channel between the two seats; there is no direct link.

It leads with **this seat's** identical error — dropping the same `(1 − q)` factor
in the opposite direction, hours earlier, logged in 035 — because the finding is
not that one seat checks the other. It is that the same factor caught both seats in
one night.

Contents, all sourced from 048:
- the `(1 − q)` diagnosis, residual 5.7e-42
- the three files to revert
- the three-way status of `1 − 1/√2` — including the row KESTREL does not have,
  that it is `−R(2,2) = −(2^s − 1)`, **PROVED** in 039 and no longer a fit
- confirmation of KESTREL's corrected R (50 digits, independent method) and c₂
- the four-family slope table, ratios `{1, −½, −3/2, +¾}`, and that the scar page's
  `18.3` was correct for the `(1,b,b)` family rather than a fitted guess
