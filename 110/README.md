# 110 — W1 resolved: the Burgers name denied, the coboundary structure awarded

**Sealed 2026-09-06, Ash + Claude (Fable seat), on Ash's word: "seal it."**

Closes work item W1 of the Shape of the Lie round-4 memo (sealed in 106): construct
the loop, transport law, and target group whose composed transport yields ε₀ — earn
or deny the Burgers name. Contents:

- `w1-report.md` — the resolution. **Denied, provably:** the candidate transport (the
  sealed shift↔character involution, 097) is an exact identity with σ² = 1 — every
  loop built from it closes perfectly, so no composition residual exists for ε₀ to
  be; the sealed 55-digit two-route agreement IS this trivial holonomy. **Awarded
  instead:** the first fully worked physics instance of the coboundary test (Shape
  of the Lie v2 §12). The energy's disagreement with its mirror image splits: the
  odd slope s_odd = −18.325965 is the COBOUNDARY (chart-removable, as 097 sealed);
  ε₀ = 5.4595046537e-4 is the CLASS (invariant under every recalibration); the
  visible crossing offset x* = 2.9791118229e-5 is the class's chart-shadow, obeying
  the arrow identity **x*·|s_odd|/ε₀ = 1.00000095 = 1 + c₂·x*/|s_odd|** (c₂ from
  sealed 096) — predicted and observed agreeing to all eight displayed digits.
- **Sealed-054 loose end closed:** 054 recorded two zeros (b* = 1.0000298,
  b₀ = 0.99997). Fresh computation finds exactly ONE zero per chart, and
  1/b* = 0.9999702093 = b₀ to every recorded digit — the two zeros are one object
  seen from the two charts. The shadow flips sign with the chart, which is exactly
  how a chart-shadow must behave and exactly how a Burgers vector may not.
- `w1_arrow_check.py` + `w1_arrow_check_output.txt` — the receipts: fresh float64
  Ewald implementation (closed-form incomplete-gamma tails, no quadrature),
  agreeing with the sealed dps-25 instrument (v_landscape.py, run in parallel the
  same day) to 3e-11 at the cube and with its CSV rows to ~1e-14; independently
  recovers 096's measured well curvature c₂ to 6 digits (code-independence check —
  same Ewald mathematics, different implementation, honestly labeled).

Chain context: 106 (the sector is the glue) → 107 (the law's d=8 defect IS the E8
glue count) → 108 (the lemma; chain proved end to end) → 110 (this entry: the miss
ε₀ is not a loop residue but an invariant class, with its chart-shadow law).

Attribution: denial criterion — the round-4 adjudication (Greg's demand, accepted,
sealed 106); fold/chart structure and exactness — sealed 053/097/098, zeros recorded
in 054 (Ash's corpus); fresh implementation, receipts, denial proof, coboundary
identification — Claude (Fable seat), 2026-09-06.
