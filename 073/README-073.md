# 073 — the human+AI handoff, and a correction to what this seat said an hour ago

**2026-08-25.** Ash: *"both AI and human. that's the THING. ok handoff for the other
AI's and humans?"*

`HANDOFF-human-and-ai.md` is that document. It is the **process** handoff; 066 is
the mathematics one.

## First, an erratum against this seat's own assessment

An hour before writing it, this seat told Ash:

> *"Not one correction of consequence was self-caught. Not one."*

**That is false, and the error log refutes it.** Seven numerical errors *were*
self-caught — the floor-at-ten, the saturated PSLQ, the tolerance-versus-truncation,
the 11-lobe counter, the truncated theta, and both bad `ℝP³` integrations.

**The overstatement is itself a Tier-2 error**: an emphasised summary sentence
contradicted by the record it summarises. Written while describing that exact
failure mode. Recorded here rather than quietly narrowed.

## What the corrected structure is

Sorting all ~17 errors by who caught them gives three tiers, and **no participant
ever caught an error outside their tier**:

| tier | what | caught by |
|---|---|---|
| **1 · numerical** | wrong arithmetic, bad integration, saturated PSLQ | the seat that made it — but **only via a test**, never by rereading |
| **2 · prose** | an emphasised sentence contradicting the table beside it | **another seat. Never once self-caught.** |
| **3 · framing** | the apparatus pointed at the wrong question | **the human. Every time. No seat ever produced one.** |

Three Tier-2 instances, all this seat's: `76%` against its own `79.0%`; *"R sits
under 1/24"* against its own `ε = +0.000546`; *"each blind exactly where the other
sees"* against its own bins `99 / 109`. The last one contradicts the word
**"independent"** written one paragraph earlier in the same entry.

Eight Tier-3 moves, all Ash's, and **not one is a correction** — every one is a
*question* that pointed the work somewhere it was not looking.

## The load-bearing sentence

> **The thing that produces the error is the thing that would have to notice it.**

Which is why the four-word ledger, the append-only vault and the visible retractions
are worth more than any single result inside them.

## Contents

The three tiers with the full error log; why each tier behaves as it does; the seven
protocols that were earned by specific failures (four words · discard don't patch ·
digit budgets · print the cut · λ-invariance · state the chart · never edit a seal);
what did not work; the claim-identity open problem with the executable-witness
proposal; and joining instructions that ask the reader to find an error rather than
agree.
