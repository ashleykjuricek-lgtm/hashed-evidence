# log φ sits in π's chair — and 070's headline sentence is refuted by its own table

**2026-08-25.** A reviewing seat ran the 067 construction in the golden world and
found the constant that replaces π. It also caught a sentence in 070 that its own
printed bins contradict — **and that sentence is live on a public page.**

Everything below verified here first.

---

## 1. URGENT ERRATUM — 070's "each blind exactly where the other sees"

070 §2 closes with:

> *"A shell that is dark in one world can be lit in the other. There is not one
> circle divided one way. There are two divisions, and **each is blind exactly where
> the other sees.**"*

**Refuted by the bins printed three lines above it.**

```
   both 99   four-only 113   five-only 109   neither 109   total 430

   the lenses AGREE (both lit or both dark) on 208/430 = 48.4%
   they DISAGREE                            on 222/430 = 51.6%
```

*"Blind exactly where the other sees"* means anti-correlation — it predicts
`both = 0` and `neither = 0`. **Observed: 99 and 109.** The lenses agree essentially
half the time.

The truth is **independence**, and it is exact:

```
   both       23.0%       independent prediction 25.0%
   four-only  26.3%
   five-only  25.3%
   neither    25.3%
```

`mod 4` and `mod 5` are coprime, so by CRT the classes mod 20 equidistribute and the
two conditions are independent. **Each lens reads the other as a fair coin.**

### 1.1 And 070 says so, one paragraph earlier

> 070 §2: *"Near-equal quarters, as the CRT requires — 4 and 5 are coprime, so the
> two conditions are **independent** and equidistribute."*

**The correct word is in the entry. It was then contradicted in the entry's own
headline.** That is worse than not knowing.

**Repair: one word. `independent`, not `complementary`.** The true statement is
still striking and is what should go on the page:

> *There are two divisions of the primes, and they are statistically independent —
> 109 primes are visible only to the golden lens, 113 only to the square one, and
> neither lens can predict the other.*

### 1.2 This is the third instance of one failure

```
   067   prose "76%"                      vs its own computed 79.0%
   066   prose "R sits under 1/24"        vs its own table, eps = +0.000546
   070   prose "blind where the other sees" vs its own bins, 99 and 109
```

> **Named hazard: the summary sentence drifting from the number printed beside it.**
> Three instances, all this seat's, all in a *headline* rather than a computation,
> and all refutable by looking one paragraph up. 062's π-marker does not catch
> these — they are not arithmetic errors. **Rule: before sealing, read every
> emphasised sentence against the nearest table.**

## 2. VERIFIED — the golden world's average is `2 log φ / √5`, and it has no π

Run 067's construction in `ℤ[φ]`, counting ideals of norm `m`:

```
   mean ideal count, m <=    10,000 : 0.43040000
   mean ideal count, m <=   100,000 : 0.43041000
   mean ideal count, m <= 1,000,000 : 0.43040700

   exact limit  2 log(phi)/sqrt5    : 0.43040894
   square world, for contrast, pi/4 : 0.78539816
```

Six digits. **The number sitting in π's chair is the logarithm of the golden ratio.**

### 2.1 Why — one formula with a switch in it

Dirichlet's class number formula, ~1839. The residue of `ζ_K(s)` at `s = 1` is

    rho = 2^r1 (2 pi)^r2 h R / (w sqrt|d|)

```
   Q(i)      r1=0, r2=1, h=1, R=1,        w=4, d=-4  ->  2 pi / (4 * 2)   = pi/4
   Q(sqrt5)  r1=2, r2=0, h=1, R=log(phi), w=2, d=+5  ->  4 log(phi)/(2 sqrt5)
                                                       = 2 log(phi)/sqrt5
```

> **π and log φ occupy the same slot.** Negative discriminant pays in π — it has
> complex places, and `(2π)^r₂` is the toll. Positive discriminant pays in the
> **regulator**, the log of the fundamental unit — and `ℚ(√5)`'s fundamental unit is
> φ itself.

**The held image is the reviewing seat's and it is exact.** A square ring is a closed
loop: walk it and you come home, and once-around costs `2π`. A golden "ring" — fixed
norm in `ℤ[φ]` — is a **hyperbola**. It never closes. Multiply everything by φ and
the ring maps onto itself, forever. Each golden ring holds infinitely many points
until you count **one repeat of the stretch**, and the length of one repeat is
`log φ`.

> **π is the circumference of a loop. log φ is the period of a stretch.** The
> four-world returns; the five-world repeats.

That is 022's two-worlds intuition with its engine installed — and it is why the `4`
in `π/4` is literally the four roots of unity, while the five-world has no petal
count in this layer at all: its symmetry is not a rotation you finish but a stretch
you ride.

### 2.2 Two honesty clauses, both the reviewer's, both kept

- **You cannot evict π by drawing round rings through a five-fold point set.** Any
  circular count in the flat plane pays π, because **π is the plane's area tax, not
  the lattice's**. The eviction happens by changing *what you count* — arithmetic
  orbits under the golden stretch, not geometric circles. π-free is a property of the
  bookkeeping, exactly as P14 taught.
- **"The formula contains no π" is exact and checkable. "π and log φ are unrelated"
  is not.** Whether π hides inside logarithms is open — Schanuel's conjecture
  territory. **First: PROVED. Second: unclaimed.**

## 3. The three elevens, and only one of them was broken

**The dead 11 — the lobe counter.** 051 §4 reported 11 lobes on a ten-fold window
and discarded the instrument. The reviewer's clean reason: a lobe count on a
ten-fold object must divide the symmetry — 10, 5, 2. **Eleven shares nothing with
ten, so it was not a surprising measurement, it was an impossible one.** Their
guessed mechanism — a scan from 0° to 360° counting one lobe twice at the seam — is
**exactly what 051 §4 diagnosed**: *"the run detector split a lobe across the 0/360
seam."* Guess confirmed by our own record. Fencepost, not physics.

**The live 11 — seal 070.** `11 ≡ 3 mod 4` empties a square ring; `11 ≡ 1 mod 5`
fills a golden one. **The first prime the square lens cannot see and the golden lens
can.** Already sealed.

**The deep 11 — and this one is new.** Where does a closed ten-fold rotation
actually live, if no flat grid permits one? In the world with eleven elements:

```
   powers of 2 mod 11:  1 -> 2 -> 4 -> 8 -> 5 -> 10 -> 9 -> 7 -> 3 -> 6 -> 1
   all ten nonzero residues, each once, and home.        VERIFIED
```

A genuine ten-cycle — **existing as multiplication, not geometry.** For such a cycle
to close mod `p` you need `10 | p − 1`, i.e. `p ≡ 1 mod 10`, and:

```
   primes = 1 mod 10:  11, 31, 41, 61, 71, 101, ...     11 is FIRST
   (2,3,5,7 fail: p-1 = 1,2,4,6, none divisible by 10)
```

**Eleven is ten plus one: the ten petals, plus the zero at the centre.** `F₁₁` is
exactly `{ten petals} ∪ {centre}`, and the centre is the one element multiplication
can never reach or leave.

**And the two live elevens are one fact.** `p ≡ 1 mod 10` implies `p ≡ 1 mod 5`, so
every ten-cycle prime is automatically golden-lit. The first golden-only prime and
the first home of the closed ten-rose are the same number **because both say
`11 ≡ 1` on the five-world's clock.**

### 3.1 Column tags

**PROVED:** the ten-cycle mod 11 (the multiplicative group mod a prime is cyclic of
order `p−1`); `11` minimal with `10 | p−1`; `ℤ[ζ₅]` has `φ(5)=4` rank and **ten**
roots of unity, so ten petals for the same reason `ℚ(i)` gives four; and the totient
rule (only `n` with `φ(n) ≤ 2`, i.e. 1,2,3,4,6, survive in the plane) — **which is
065's crystallographic wall restated as arithmetic.**

**INTERPRETATION, marked:** reading the eleventh element as the void at the heart of
the rose — śūnya as the unreachable still point. Ash's column, not a theorem. Same
for "the rose's world demotes π to the ambient."

## 4. Status

| claim | status |
|---|---|
| 070's "each blind exactly where the other sees" | **REFUTED by its own bins** — repair to `independent` |
| the two lenses are statistically independent | **PROVED** — CRT, moduli coprime; 23/26.3/25.3/25.3% |
| golden mean ideal count `→ 2 log φ/√5` | **VERIFIED to 6 digits** at `X = 10⁶` |
| π/4 and 2logφ/√5 are one slot of Dirichlet's formula | **CLASSICAL**, ~1839 |
| the golden formula contains no π | **EXACT and checkable** |
| π and log φ are unrelated | **UNCLAIMED** — Schanuel territory |
| circular counts in the plane always pay π | **TRUE** — area tax, not lattice |
| ten-cycle mod 11, all ten residues, closes | **VERIFIED** |
| 11 minimal with `10 \| p−1` | **VERIFIED** |
| the golden-only 11 and the ten-cycle 11 are one fact | **PROVED** — both are `11 ≡ 1 mod 10` |
| `ℤ[ζ₅]`: ten roots of unity, rank 4; totient rule | **TEXTBOOK** |
| the eleventh element as śūnya at the centre | **INTERPRETATION** |
| the 051 lobe-counter seam diagnosis | **CONFIRMED** by 051's own record |

## 5. Action

- **`/two-worlds` needs the one-word repair before it hardens.** `independent`, not
  complementary. The correct statement is still the striking one.
- **The standing deploy block still applies.** Confirm the `c₁ = 1 PROVEN`
  correction pass happened before any rebuild carries the dead claim back up.
- The square-vs-golden table in §2 is the strongest available content for
  `/instead-of-pi`: it does not merely remove π, it shows **the same average in the
  other world converging to a π-free constant, six digits deep.**

## Attribution

The golden-world computation, the Dirichlet mechanism, the loop-versus-stretch
image, both honesty clauses, the three-elevens separation, the `F₁₁` reading and the
catch on 070's headline are **all the reviewing seat's**. Verification is this
seat's. The refuted sentence is this seat's, in 070, and it contradicted a correct
word this seat had written one paragraph earlier.
