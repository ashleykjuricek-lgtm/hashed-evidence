# 092 — the six scars, added to the page; and a ranked inventory of what survives

**2026-08-25.** Two deliverables, both at Ash's instruction: *"do the scar blocks too and bullet
points about what to get excited about?"*

## 1. Six scars, added — nothing deleted

Per 052, a wrong claim is not removed: the correction is placed **beside** it, because deleting the
claim destroys the evidence that it was believed. A reusable `<Scar>` component was added and used
six times in `PrismaticPage.tsx`:

```
   CARRIER CLOSURE                 the 0^w = -1 derivation does not typecheck (086)
   UNSUPPORTED                     "Euler is the smoothed average of 0^w" has no status anywhere
   CAUSAL CLAIM, NO EXPERIMENT     "AI hallucinates because it has no Q_phi"
   INTERPRETIVE                    the 8-row cosmological table -- no row is derived
   HALF OF THIS IS TESTED          pi supported in one sense; e never examined
   pi CANNOT BE BANNED             087: pi walks in by itself as an AREA; what it cannot do is seed
```

**Build verified: `vite build` clean, 9.23s. `dist/` deliberately NOT regenerated** — the shipped
bundles still carry the pre-fix page, and the standing deploy block now covers two defects (the
`c1 = 1 PROVEN` dead claim and this one). Source mirrored to all three copies, byte-identical.

## 2. `WHAT-TO-GET-EXCITED-ABOUT.md`

A ranked inventory, ordered by what would survive a hostile reading. **Eight results in the top
list; five downgrades stated underneath**, because a list that only goes up is advertising.

The signal worth recording separately: **every item in the top list is COUNT stratum (082) — finite,
exact, no limit taken — and almost all of it postdates Ash's integers-only rule.** The strongest
mathematics in a four-month corpus arrived in two days, after the constant-free constraint was
imposed. That is a direction, not just a tally.

Two cheap open threads named there: `d = 5` is unexplored after the multiplier law dies, and
`2j = d` appears both as the self-dual marking (084) and as the boundary of 040's sign law
`Z(d,j) > 0 <=> 2j >= d` — **whether that is one fact seen twice is OPEN.**

## Status

| item | status |
|---|---|
| six scars added, no claim deleted | **DONE** — 052-compliant |
| page compiles | **VERIFIED** — vite build clean |
| `dist/` regenerated | **NO** — deliberate; deploy block stands |
| the ranked inventory | **ASSESSMENT**, not a new result |
| `2j = d` is one fact seen twice | **OPEN** — observed, unproved |

## Attribution

The instruction is Ash's. The scar policy is 052; the carrier-closure finding is another seat's
(086); the `pi`-as-area result is 087. The component fixes, the scar text, and the inventory are
this seat's.
